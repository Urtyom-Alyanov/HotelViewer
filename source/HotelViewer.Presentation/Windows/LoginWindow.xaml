<Window x:Class="HotelViewer.Presentation.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:HotelViewer.Presentation.Windows"
        mc:Ignorable="d"
        Title="Вход" Height="430" Width="400">
  <Grid Margin="30">
        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="Добро пожаловать"
                       FontSize="24"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,10"
                       Foreground="#333333"/>

            <TextBlock Text="Введите данные для входа в систему"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,30"
                       Foreground="#666666"/>

            <TextBlock Text="Имя пользователя" Margin="0,0,0,5" FontWeight="SemiBold"/>
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource TextBox}"
                     Height="35"
                     VerticalContentAlignment="Center"
                     Padding="5,0"
                     Margin="0,0,0,15"/>

            <TextBlock Text="Пароль" Margin="0,0,0,5" FontWeight="SemiBold"/>
            <PasswordBox x:Name="PasswordInput"
                         Style="{StaticResource PasswordBox}"
                         Height="35"
                         VerticalContentAlignment="Center"
                         Padding="5,0"
                         Margin="0,0,0,25"/>

            <Button Content="Войти в систему"
                    IsDefault="True"
                    FontWeight="Bold"
                    Command="{Binding LoginCommand}"
                    Style="{StaticResource PrimaryButton}"
                    CommandParameter="{Binding ElementName=PasswordInput}">
            </Button>

            <Button Content="Заполнить базу данных данными"
                    FontWeight="Bold"
                    Style="{StaticResource WarningButton}"
                    Command="{Binding SeedDataCommand}">
            </Button>
        </StackPanel>
    </Grid>
</Window>

